# Frontend Environment Variables

# The Base URL of the backend API (e.g. deployed Render URL or localhost)
VITE_API_BASE_URL=http://localhost:3001/api

# The WebSocket URL of the backend server (e.g. deployed Render URL or localhost)
VITE_WS_URL=http://localhost:3001
